import * as React from "react";
import {nav} from './AppNav.module.css'

function AppNav() {
    return (
        <nav className={nav} >
            AppNav
        </nav>
    )
}

export default AppNav
